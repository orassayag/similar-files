import { Settings } from './types';

export const settings: Settings = {
  scanPath: '/Users/orassayag/Downloads',
  ignorePaths: ['node_modules', '.git', 'Library', 'Applications'],
  sizeToleranceBytes: 100,
  dryMode: true,
};
